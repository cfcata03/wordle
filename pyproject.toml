[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "wordle-clone"
version = "1.0.0"
description = "A Wordle clone with a dark-themed Tkinter GUI"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["src"]
